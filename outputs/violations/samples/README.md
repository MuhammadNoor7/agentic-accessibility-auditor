# Rule fixture violation samples (R01–R30)

Curated `*_violations.json` outputs from `python test_run.py --fixtures`.

| Coverage | Detail |
|----------|--------|
| Rules | R01–R27, R29–R30 (56 files; **R28** has no XML fixture yet) |
| Source XML | `tests/fixtures/rules/` |
| Regenerate | `python test_run.py --fixtures` |

Full MASC batch outputs stay local under `outputs/violations/` and are **not** committed.
