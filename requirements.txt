streamlit>=1.36.0
lxml>=5.2.0
jsonschema>=4.23.0
pytest>=8.0.0
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.9
Pillow
google-genai
Jinja2
playwright

# Stage 3 explainer (src/explainer.py, src/llm_providers.py) - install only the
# SDK for the LLM_PROVIDER you actually use (see .env.example).
anthropic
openai
groq

# CV models (src/crop_violation_classifier.py, src/yolo_ui_detector.py).
# torch/torchvision cover the torchvision-native backbones (mobilenet_v3_*,
# efficientnet_*, resnet50, vit_b_16, etc.); timm additionally covers the
# backbone-sweep architectures torchvision doesn't ship (mobilevit*,
# mobilenetv1/v4, ghostnet*, mobileone*, swin_tiny, convnext_tiny, ...).
torch>=2.11.0
torchvision>=0.26.0
timm>=1.0.0
ultralytics>=8.3.0
